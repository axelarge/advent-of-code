(ns advent-of-code.{YYYY}.day{DD}-test
  (:require [clojure.test :refer :all]
            [advent-of-code.{YYYY}.day{DD} :refer :all]))

(deftest test-solve1
  (is (= (solve1 input) nil)))

(deftest test-solve2
  (is (= (solve2 input) nil)))
